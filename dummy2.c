#include <stdio.h>
#include <stdlib.h>

int main() {

    int i,n,a[50],sum;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    
    }
        for(i=0;i<n;i++)
        {
          sum+=i;}
    
    printf("%d\n",sum);
    
    return 0;
}
